---
sidebar_position: 42
---

# getblockhash
## Command

```bash
pacprotocol-cli getblockhash height
```

Returns hash of block in best-block-chain at height provided.



## Arguments

### `height` <span class='asterisk'>*</span>
*(numeric, required)*

The height index


## Examples
```bash
pacprotocol-cli getblockhash 1000
```

```bash
curl --user myusername --data-binary '{"jsonrpc": "1.0", "id":"curltest", "method": "getblockhash", "params": [1000] }' -H 'content-type: text/plain;' http://127.0.0.1:1111/
```
## Result
```bash
"hash"         #(string) The block hash
```


___

Running following help command
```bash
 pacprotocol-cli help getblockhash
```
Will produce following output:


```bash
getblockhash height

Returns hash of block in best-block-chain at height provided.

Arguments:
1. height         (numeric, required) The height index

Result:
"hash"         (string) The block hash

Examples:
> pacprotocol-cli getblockhash 1000
> curl --user myusername --data-binary '{"jsonrpc": "1.0", "id":"curltest", "method": "getblockhash", "params": [1000] }' -H 'content-type: text/plain;' http://127.0.0.1:1111/

```

