---
sidebar_position: 161
---

# walletpassphrasechange
## Command

```bash
pacprotocol-cli walletpassphrasechange "oldpassphrase" "newpassphrase"
```

Changes the wallet passphrase from 'oldpassphrase' to 'newpassphrase'.



## Arguments

### `oldpassphrase` 
*(string)*

The current passphrase


### `newpassphrase` 
*(string)*

The new passphrase


## Examples
```bash
pacglobal-cli walletpassphrasechange "old one" "new one"
```

```bash
curl --user myusername --data-binary '{"jsonrpc": "1.0", "id":"curltest", "method": "walletpassphrasechange", "params": ["old one", "new one"] }' -H 'content-type: text/plain;' http://127.0.0.1:1111/
```


___

Running following help command
```bash
 pacprotocol-cli help walletpassphrasechange
```
Will produce following output:


```bash
walletpassphrasechange "oldpassphrase" "newpassphrase"

Changes the wallet passphrase from 'oldpassphrase' to 'newpassphrase'.

Arguments:
1. "oldpassphrase"      (string) The current passphrase
2. "newpassphrase"      (string) The new passphrase

Examples:
> pacglobal-cli walletpassphrasechange "old one" "new one"
> curl --user myusername --data-binary '{"jsonrpc": "1.0", "id":"curltest", "method": "walletpassphrasechange", "params": ["old one", "new one"] }' -H 'content-type: text/plain;' http://127.0.0.1:1111/

```

